(executable
  (name main)
  (public_name x86emulator)
  (modules
   main
  )
  (libraries
    core
    x86emulator
    x86emulator.config
    x86emulator.emulator
  )
  (preprocess (pps ppx_jane))
  (package x86emulator))
